  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav>
    <% if (user) { %>
      <div id="after-login-nav">
      <a href="/posts">
        <button type="submit">Stuff</button>
      </a>
      <a href="/profiles">
        <button type="submit">Profiles</button>
      </a>
      <a href="/auth/logout">
        <button type="submit">Logout</button>
      </a>
      </div>
    <% } else { %>
      <a id ="login-navbar" href="/auth/google" class="siwg-link">
        <button type="submit" class="siwg">
          <img id="siwg-image" alt="Sign in with Google" />
        </button>
      </a>
    <% } %>
  </nav>

